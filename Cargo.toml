[package]
name = "xml-mcp"
version = "0.2.0"
edition = "2021"

[dependencies]
quick-xml = "0.37"
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
glob = "0.3"

[profile.release]
opt-level = 2
lto = "thin"
